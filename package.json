{
  "name": "code-cut",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter server dev\" \"pnpm --filter ui dev\"",
    "build": "pnpm --filter ui build && pnpm --filter server build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "tsx": "^4.19.2"
  }
}
